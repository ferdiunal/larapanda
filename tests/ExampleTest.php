<?php

declare(strict_types=1);

/**
 * Baseline sanity test for Pest bootstrap validation.
 */
/**
 * Verify testing environment executes assertions correctly.
 */
it('can test', function () {
    expect(true)->toBeTrue();
});
